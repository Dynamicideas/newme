#assign a to 1, b to 5, and c to 3. using the quadratic
#equation, find the possible values of x

a <- c(1)
b <- c(5)
c <- c(3)

x1 <- c((-b+sqrt(b^2-(4*a*c)))/(2*a))
x1        

x2 <- c((-b-sqrt(b^2-(4*a*c)))/(2*a))        
x2
